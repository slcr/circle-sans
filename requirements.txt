# Pinned deliberately. These are the exact versions the fonts in the last
# release were built with. Unpinned ranges make pip backtrack through very old
# fontbakery releases in CI, and the oldest of those need fontforge to even
# generate metadata, which fails the build. Bump with `make update`.
fontmake==3.12.1
gftools[qa]==0.9.996
fontbakery==1.1.0
fonttools==4.63.0
glyphsLib==6.14.0
drawbot-skia==0.5.1
sh==2.4.0
bumpfontversion==0.4.1
diffenator2==0.5.0
